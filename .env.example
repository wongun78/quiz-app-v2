# Google Cloud Configuration
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
GCP_ZONE=us-central1-a

# Database Configuration (Non-sensitive - safe in .env)
DB_PRIVATE_IP=your-db-private-ip
DB_NAME=quiz_db
DB_USER=quizadmin

# DB_PASSWORD - Managed by Secret Manager (db-password)
# JWT_SECRET - Managed by Secret Manager (jwt-secret)
# ADMIN_PASSWORD - Managed by Secret Manager (admin-password)
# USER_PASSWORD - Managed by Secret Manager (user-password)

# For initial setup only (setup-quiz.sh will create secrets):
DB_PASSWORD=your-secure-password

# Redis Configuration (Non-sensitive)
REDIS_HOST=your-redis-private-ip
REDIS_PORT=6379

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://quiz-frontend-xxx.us-central1.run.app

# Cloud Storage (if needed)
BUCKET_NAME=your-bucket-name
